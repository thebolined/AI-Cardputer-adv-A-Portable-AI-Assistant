# Copy this file to your SD card root as config.txt and fill in your values.
# Lines starting with # are ignored. Leave values empty to skip them.
# The file is DELETED from the SD card automatically after a successful import.
# Never commit your filled-in config.txt to git (it is covered by .gitignore).

ssid=YourWiFiName
password=YourWiFiPassword

kimi_key=
qwen_key=
ds_key=
